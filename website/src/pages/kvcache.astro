---
import BlogLayout from '../layouts/BlogLayout.astro';
---

<BlogLayout
  title="KVCache 存储"
  description="跨 DRAM 与 SSD 统一管理 KVCache，并向调度器提供全局 metadata，实现 KVCache-aware scheduling。"
  lang="zh"
  category="项目案例"
  date="2025 — 至今"
  readingTime={2}
  kind="project"
>
  <h2>项目目标</h2>
  <p>
    统一管理 DRAM/SSD 上的 KVCache，作为独立的组件 daemon，与推理框架（vLLM/sglang/MiniMax 内部框架）解耦，同时上报 KVCache metadata 给调度器做 KVCache-aware 调度。
  </p>

  <h2>核心功能</h2>
  <ul>
    <li>统一管理 DRAM + SSD 上的 KVCache</li>
    <li>作为独立 daemon 运行，与推理框架解耦</li>
    <li>KVCache metadata 上报，供调度器做感知调度</li>
  </ul>

  <h2>技术架构</h2>
  <ul>
    <li><strong>独立 Daemon</strong>：KVCache 存储作为独立组件，不依赖推理框架</li>
    <li><strong>多级存储</strong>：支持 DRAM 和 SSD 两级 KVCache 存储</li>
    <li><strong>Metadata 上报</strong>：上报 KVCache 元数据给调度器</li>
    <li><strong>框架兼容</strong>：vLLM、Sglang、MiniMax 内部推理框架</li>
  </ul>

  <h2>成果</h2>
  <ul>
    <li>节约上千卡成本</li>
    <li>支持多种推理框架</li>
    <li>实现 KVCache 感知调度</li>
  </ul>
</BlogLayout>
