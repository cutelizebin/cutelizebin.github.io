---
import BlogLayout from '../layouts/BlogLayout.astro';
---

<BlogLayout
  title="DeepSeek-V4-Pro 每个 token 的 KVCache 大小是多少？"
  description="推导 DeepSeek-V4-Pro 在 1 Mi token 上下文下的 KVCache：BF16 约 9.625 GiB，并解释 FP8/FP4 与 GQA8 基线的比较口径"
  lang="zh"
  category="模型分析"
  date="2026 年 5 月 · 2026 年 8 月修订"
  dateISO="2026-05-01"
  readingTime={7}
  canonical="https://cutelizebin.github.io/blog-dv4-pro-kvcache/"
>
  <div class="highlight">
    <div class="number">9.625 KiB/token</div>
    <div class="label">BF16 架构估算（1 Mi token 上下文）</div>
    <div class="sub">总计 ≈ 9.625 GiB · FP8/FP4 部署通常约再减半</div>
  </div>

  <h2>结论</h2>
  <ul>
    <li>在 <strong>1,048,576 token</strong> 上下文下，BF16 KVCache 的架构估算为 <strong>9.625 GiB</strong>，平均 <strong>9.625 KiB/token</strong>。</li>
    <li>vLLM 的部署说明指出，主 attention cache 采用以 FP8 为主的混合存储、indexer cache 使用 FP4 后，相比 BF16 估算还可<strong>大约再减半</strong>，即约 4.8 GiB；实际值取决于具体实现。</li>
    <li>相对 61 层、8 个 KV heads、head dimension 128 的 BF16 GQA8 基线，BF16 架构估算小 <strong>25.35×</strong>；采用 FP8/FP4 后约为 <strong>50×</strong>，对应基线的约 2%。</li>
    <li>原推导把共享 K/V 向量误算成独立的一对 KV，并将 GiB 误写成 MB；本次修订统一改为二进制单位和可复现口径。</li>
  </ul>

  <h2>数据来源与计算口径</h2>
  <ul>
    <li>模型参数来自 DeepSeek 的 <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro/blob/main/config.json" target="_blank" rel="noreferrer">DeepSeek-V4-Pro 官方配置</a>。</li>
    <li>CSA、HCA 与混合 attention 架构来自 <a href="https://arxiv.org/abs/2606.19348" target="_blank" rel="noreferrer">DeepSeek-V4 技术报告</a>。</li>
    <li>共享 K/V、BF16 的 9.62 GiB 算法及 FP8/FP4 部署口径参考 <a href="https://vllm.ai/blog/2026-04-24/deepseek-v4" target="_blank" rel="noreferrer">vLLM 的实现说明</a>。</li>
    <li>下文计算的是单条序列的架构级缓存 payload，不包含实现相关的 compressor runtime state、block padding、量化 scale、allocator metadata 和临时 workspace；复核日期为 2026-08-01。</li>
  </ul>

  <h2>关键参数</h2>
  <table>
    <tr><th>参数</th><th>值</th></tr>
    <tr><td>head_dim</td><td>512</td></tr>
    <tr><td>index_head_dim</td><td>128</td></tr>
    <tr><td>qk_rope_head_dim</td><td>64</td></tr>
    <tr><td>num_key_value_heads</td><td>1</td></tr>
    <tr><td>num_hidden_layers</td><td>61</td></tr>
    <tr><td>sliding_window</td><td>128</td></tr>
    <tr><td>max_position_embeddings</td><td>1,048,576</td></tr>
  </table>

  <h3>压缩策略</h3>
  <p>官方配置中的 <code>compress_ratios</code> 以前两层的 128 开始，之后在 4 与 128 之间交替。因此 61 层中共有 30 个 c4a（CSA）层和 31 个 c128a（HCA）层；每层还保留最近 128 个未压缩 token 的 SWA 分支。</p>

  <h2>为什么一个缓存 entry 是 1,024 字节</h2>
  <p>DeepSeek-V4 的主 attention 共享 Key 与 Value 向量，因此每个压缩或 SWA entry 只缓存一个 512 维向量，而不是分别缓存一份 Key 和一份 Value。BF16 下：</p>
  <ul>
    <li>主 attention 共享 K/V entry：512 × 2 字节 = <strong>1,024 字节</strong></li>
    <li>仅 CSA 使用的 indexer entry：128 × 2 字节 = <strong>256 字节</strong></li>
  </ul>

  <h2>BF16 各组件计算</h2>
  <p>令上下文长度 <code>N = 1,048,576</code>。</p>

  <h3>1. SWA（每层固定保留 128 个 entry）</h3>
  <pre>SWA 固定缓存 = 61 × 128 × 1,024 = 7,995,392 字节
SWA 平均每 token = 7,995,392 / N = 7.625 字节/token</pre>

  <h3>2. CSA / c4a（30 层）</h3>
  <pre>主 attention = 30 × (N / 4) × 1,024 / N = 7,680 字节/token
indexer = 30 × (N / 4) × 256 / N = 1,920 字节/token
CSA 合计 = 9,600 字节/token</pre>

  <h3>3. HCA / c128a（31 层）</h3>
  <pre>HCA = 31 × (N / 128) × 1,024 / N
    = 248 字节/token</pre>

  <h2>汇总</h2>
  <table>
    <tr><th>组件</th><th>每 token 大小</th><th>占比</th></tr>
    <tr><td>SWA @ 1 Mi token</td><td>7.625 字节</td><td>0.08%</td></tr>
    <tr><td>CSA 主 attention</td><td>7,680 字节</td><td>77.93%</td></tr>
    <tr><td>CSA indexer</td><td>1,920 字节</td><td>19.48%</td></tr>
    <tr><td>HCA 主 attention</td><td>248 字节</td><td>2.52%</td></tr>
    <tr><td><strong>总计</strong></td><td><strong>9,855.625 字节 ≈ 9.625 KiB/token</strong></td><td>100%</td></tr>
  </table>

  <p><strong>总缓存：</strong>9,855.625 字节/token × 1,048,576 token = 10,334,371,840 字节 = <strong>9.6246 GiB</strong>。</p>

  <h2>与 BF16 GQA8 基线比较</h2>
  <pre>GQA8 基线 = 61 层 × 8 KV heads × 2（K 和 V）
           × 128 维 × 2 字节
           = 249,856 字节/token
           = 244 KiB/token
1 Mi token 总量 = 244 GiB

BF16 压缩倍数 = 244 / 9.6246 = 25.35×
BF16 占基线比例 = 9.6246 / 244 = 3.94%</pre>

  <p>vLLM 的实现说明给出的部署配置是主 attention cache 采用以 FP8 为主的混合存储、indexer cache 使用 FP4，并称其相对上述 BF16 估算可再节省约 2×。据此只能给出约 <strong>4.8 GiB / 4.8 KiB per token</strong> 的量级估算，而不应写成与实现无关的精确值；该量级相当于 GQA8 基线的约 2%。</p>
</BlogLayout>
